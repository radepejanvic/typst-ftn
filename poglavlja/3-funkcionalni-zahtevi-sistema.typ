#import "../funkcije.typ": todo
= Функционални захтеви система
// <захтеви_система>

// Ово поглавље дефинише функционалне (поглавље #link(<функционални_захтеви>)[3.1]) и нефункционалне (поглавље #link(<нефункционални_захтеви>)[3.2]) захтеве система.


// Циљ је да се прецизно опишу способности и ограничења алата за аутоматизовано креирање, конфигурисање и управљање виртуелним окружењем намењеним развоју и тестирању компоненти пројекта Constellations. У оквиру функционалних захтева нагласак је на опису конкретних операција и процеса које систем мора да подржи, док нефункционални захтеви обухватају перформансе, преносивост, поузданост и једноставност употребе решења.

// == Функционални захтеви система
<функционални_захтеви>
Потребно је креирати алат за покретање _N_ виртуелних машина одједном. Свака виртуелна машина треба да има инсталирану лагану (енгл. _lightweight_) дистрибуцију _Linux_ оперативног система. Конфигурација мреже самих машина треба да буде таква да машине буду међусобно видљиве, да виртуелне машине вуду видљиве са _host_ машине, као и да _host_ машина буде видљива из виртелних машина (слика #link(<fig:arh-sis>)[5]). 

На свакој машини је потребно инсталирати систем за контролу верзија (енгл. _Version Control System_, VCS) _Git_ @git_scm_homepage, _Docker_ и _Go_ програмски језик, који представљају основно развојно окружење за компоненте _Constellations_ пројекта. Потребно је клонирати `c12s/tools` @c12s_tools  репозиторијум који садржи опште алате у оквиру пројекта _Constellations_. Након тога потребно је покренути инсталационе скрипте из `c12s/tools` репозиторијума и подесити параметре окружења како би се инсталирали неопходни сервиси. Након инсталације потребно је покренути `c12s/star` @c12s_star сервис на свакој машини (слика #link(<fig:vm>)[6]). 

На _host_ машини је потребно инталирати и покренути _control plane_ која обухвата скуп сервиса у склопу _Constellations_ пројекта који се такође ослањају на употребу `c12s/tools` репозиторијума. Након покретања виртуелних машина и подизања неопходне инфраструктуре тестирање се врши путем `c12s/cockpit` @c12s_cockpit алата који се налази у оквиру _control plane_-a. 

Конфигурација окружења треба да се дефинише помоћу YAML датотеке. Алат такође треба да подржи неколико различитих дистрибуција _Linux_ оперативног система (са и без графичког интерфејса).

#figure(image("../slike/arh-sis.svg", width: 65%),
  caption: [
    Инфраструктура локалног развојног окружења.
  ]
)<fig:arh-sis>

#figure(image("../slike/vm.svg", width: 30%),
  caption: [
    Поставка система на виртуелној машини.
  ]
)<fig:vm>



// == Нефункционални захтеви система
// <нефункционални_захтеви>
// 